import { ControlerFormDirective } from './controler-form.directive';

describe('ControlerFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlerFormDirective();
    expect(directive).toBeTruthy();
  });
});
